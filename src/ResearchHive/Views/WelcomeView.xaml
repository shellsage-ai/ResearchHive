<UserControl x:Class="ResearchHive.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource BackgroundBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="500">
            <TextBlock Text="🔬" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,16"/>
            <TextBlock Text="Welcome to ResearchHive" 
                       FontSize="28" FontWeight="Bold" 
                       Foreground="{StaticResource TextPrimaryBrush}"
                       HorizontalAlignment="Center" Margin="0,0,0,8"/>
            <TextBlock Text="{Binding WelcomeText}" 
                       FontSize="14" Foreground="{StaticResource TextSecondaryBrush}"
                       HorizontalAlignment="Center" TextAlignment="Center"
                       TextWrapping="Wrap" LineHeight="22" Margin="0,0,0,24"/>

            <Border Style="{StaticResource Card}" MaxWidth="400">
                <StackPanel>
                    <TextBlock Text="Quick Start" Style="{StaticResource SectionHeader}"/>
                    <StackPanel Margin="0,4,0,0">
                        <TextBlock Text="1. Click '+ New Session' in the sidebar" 
                                   FontSize="13" Margin="0,4" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="2. Choose a domain pack for your research area" 
                                   FontSize="13" Margin="0,4" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="3. Enter a research prompt and run the agent" 
                                   FontSize="13" Margin="0,4" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="4. Review evidence, reports, and diverse viewpoints" 
                                   FontSize="13" Margin="0,4" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}" MaxWidth="400" Margin="0,8,0,0">
                <StackPanel>
                    <TextBlock Text="Domain Packs" Style="{StaticResource SectionHeader}"/>
                    <TextBlock Text="• General Research — broad topic exploration" FontSize="12" Margin="0,2" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Text="• History / Philosophy — historical &amp; philosophical inquiry" FontSize="12" Margin="0,2" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Text="• Math — mathematical research and proofs" FontSize="12" Margin="0,2" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Text="• Maker / Materials — materials science with safety" FontSize="12" Margin="0,2" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Text="• Chemistry (Safe) — chemistry with safety labels" FontSize="12" Margin="0,2" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Text="• Programming / IP — software research with license analysis" FontSize="12" Margin="0,2" Foreground="{StaticResource TextSecondaryBrush}"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
